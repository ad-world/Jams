import Queue from "./models/queue.model";
import { db } from "./db";
import { getAccount } from "./account";

export const createSession = async (accountId?: string) => {
  //   const queue: Queue = {
  //     hostId: accountId,
  //     hostName: account.
  //   };
  //   const result = await db().collection<Queue>("queues").insertOne();
};
